//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { FlightService } from '@/services/Flight.service';

const useDeleteAllFlight = (onSuccess: (result: string[]) => void, onError: (e: Error | AxiosError) => void) => {
  const queryClient = useQueryClient();

  const {
    mutate: deleteAllFlight,
    mutateAsync: deleteAllFlightAsync,
    isPending,
  } = useMutation({
    mutationFn: (ids: string[]) => FlightService.DeleteAllFlight(ids),
    onSuccess: (_, ids) => {
      const className = 'flight';
      ids.forEach((id) => {
        queryClient.removeQueries({ queryKey: [className, id] });
      });
      queryClient.invalidateQueries({ queryKey: [className, 'list'] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(ids);
    },
    onError: (e) => onError(e),
  });

  return { deleteAllFlight, deleteAllFlightAsync, isPending };
};

export default useDeleteAllFlight;
