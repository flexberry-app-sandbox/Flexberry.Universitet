//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { HelicopterService } from '@/services/Helicopter.service';
import { IBaseHelicopter } from '@/types/Helicopter.types';

const useUpdateHelicopter = <T extends IBaseHelicopter>(
  onSuccess: (result: T) => void,
  onError: (e: Error | AxiosError) => void
) => {
  const queryClient = useQueryClient();

  type UpdateRecordArgs = {
    helicopter: T;
    viewName: string;
  };

  const {
    mutate: updateHelicopter,
    mutateAsync: updateHelicopterAsync,
    isPending,
  } = useMutation({
    mutationFn: (updateRecordArgs: UpdateRecordArgs) =>
      HelicopterService.UpdateHelicopter<T>(updateRecordArgs.helicopter, updateRecordArgs.viewName),
    onSuccess: (response, variables) => {
      const id = response.data.id;
      const { viewName } = variables;
      const className = 'helicopter';
      queryClient.invalidateQueries({ queryKey: [className, id] });
      queryClient.invalidateQueries({ queryKey: [className, 'list', viewName] });
      onSuccess(response.data);
    },
    onError: (e) => onError(e),
  });

  return { updateHelicopter, updateHelicopterAsync, isPending };
};

export default useUpdateHelicopter;
