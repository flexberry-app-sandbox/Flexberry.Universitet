//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useEffect } from 'react';
import { isAxiosError } from 'axios';
import { useQuery } from '@tanstack/react-query';

import { HelicopterService } from '@/services/Helicopter.service';
import type { IBaseHelicopter } from '@/types/Helicopter.types';
import type { GetRequestOptions } from '@/types/helpers/GetRequestOptions';

const useGetHelicopter = <T extends IBaseHelicopter>({
  id,
  viewName,
  enabled = true,
  onNotFound,
}: GetRequestOptions) => {
  const { data, isLoading, isError, error } = useQuery({
    queryKey: ['helicopter', id],
    retry: false,
    enabled: enabled,
    queryFn: async (): Promise<T> => {
      const response = await HelicopterService.GetHelicopter(id, viewName);
      return response.data;
    },
  });

  useEffect(() => {
    if (isError && isAxiosError(error)) {
      if (typeof onNotFound === 'function' && error.response?.status === 404) {
        onNotFound();
      }
    }
  }, [error, isError, onNotFound]);

  return {
    data,
    isLoading,
    isError,
  };
};

export default useGetHelicopter;
