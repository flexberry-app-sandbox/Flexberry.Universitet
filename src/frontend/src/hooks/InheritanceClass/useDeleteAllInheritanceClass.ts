//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { InheritanceClassService } from '@/services/InheritanceClass.service';

const useDeleteAllInheritanceClass = (
  onSuccess: (result: string[]) => void,
  onError: (e: Error | AxiosError) => void
) => {
  const queryClient = useQueryClient();

  const {
    mutate: deleteAllInheritanceClass,
    mutateAsync: deleteAllInheritanceClassAsync,
    isPending,
  } = useMutation({
    mutationFn: (ids: string[]) => InheritanceClassService.DeleteAllInheritanceClass(ids),
    onSuccess: (_, ids) => {
      const className = 'inheritanceClass';
      ids.forEach((id) => {
        queryClient.removeQueries({ queryKey: [className, id] });
      });
      queryClient.invalidateQueries({ queryKey: [className, 'list'] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(ids);
    },
    onError: (e) => onError(e),
  });

  return { deleteAllInheritanceClass, deleteAllInheritanceClassAsync, isPending };
};

export default useDeleteAllInheritanceClass;
