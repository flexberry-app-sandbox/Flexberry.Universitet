//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file, except  programmer edit sections, will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { AxiosError } from 'axios';

import { AssosiationClassService } from '@/services/AssosiationClass.service';

const useDeleteAllAssosiationClass = (
  onSuccess: (result: string[]) => void,
  onError: (e: Error | AxiosError) => void
) => {
  const queryClient = useQueryClient();

  const {
    mutate: deleteAllAssosiationClass,
    mutateAsync: deleteAllAssosiationClassAsync,
    isPending,
  } = useMutation({
    mutationFn: (ids: string[]) => AssosiationClassService.DeleteAllAssosiationClass(ids),
    onSuccess: (_, ids) => {
      const className = 'assosiationClass';
      ids.forEach((id) => {
        queryClient.removeQueries({ queryKey: [className, id] });
      });
      queryClient.invalidateQueries({ queryKey: [className, 'list'] });
      queryClient.invalidateQueries({ queryKey: [className, 'count'] });
      onSuccess(ids);
    },
    onError: (e) => onError(e),
  });

  return { deleteAllAssosiationClass, deleteAllAssosiationClassAsync, isPending };
};

export default useDeleteAllAssosiationClass;
